function subscribe() {
    var name = document.getElementById("name").value;
    var email = document.getElementById("email").value;

    // You can perform additional validation here if needed

    alert("Thank you, " + name + "! You have subscribed with email: " + email);
}
